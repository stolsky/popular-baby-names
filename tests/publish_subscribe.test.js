import {
    beforeEach,
    describe,
    expect,
    it
} from 'vitest'

import { notify, subscribe } from '../src/publish_subscribe/broker'

describe('test subscribe', () => {
    it('subscribe with theme', () => {

    })
    it('subscribe without theme', () => {

    })
})

describe('test notify', () => {
    it('get notified by theme, if data connected to this theme changed', () => {

    })
    it('get not notified by theme, if data connected to another theme changed', () => {

    })
})
